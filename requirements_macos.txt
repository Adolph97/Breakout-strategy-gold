# Cross-platform dependencies for Gold Trader Bot (macOS/Linux compatible)
# For price data, we'll use yfinance as a free alternative to MT5 data feed
# For execution on macOS, you would need to connect to a Windows MT5 instance via API
# or use a different broker's API

# Core data processing and analysis
pandas==2.2.0
numpy==1.26.4
requests==2.31.0
beautifulsoup4==4.12.2
Flask==3.0.2
pytz==2024.1

# Alternative to MetaTrader5 for price data (works on macOS)
yfinance==0.2.38

# Optional: For development and testing
# pytest==7.4.3
# black==23.12.0
# flake8==6.1.0